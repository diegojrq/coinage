<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @account.name %>
</p>

<h2>Transactions</h2>
<%= render @account.transactions %>

<h2>Add a Transaction:</h2>
<%= render "transactions/form_account" %>

<%= link_to 'Edit', edit_account_path(@account) %> |
<%= link_to 'Back', accounts_path %>
